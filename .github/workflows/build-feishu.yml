name: Build ZeroClaw (ARM64, channels-full)

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: zeroclaw-full

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          repository: zeroclaw-labs/zeroclaw
          ref: master
          submodules: true
          fetch-depth: 1

      - name: Get patched Dockerfile
        run: curl -sL "https://raw.githubusercontent.com/lsynpy/zeroclaw/main/Dockerfile" > Dockerfile

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest
          build-args: |
            ZEROCLAW_CARGO_FEATURES=channels-full
          cache-from: type=gha
          cache-to: type=gha,mode=max
